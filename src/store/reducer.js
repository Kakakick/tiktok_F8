const initState = {
    todo: [],
    todoInput: ''
};

const ADD_ACTION = 'add';
const REMOVE_ACTION = 'remove'

function reducer(state, action) {
    switch (action) {
        case ADD_ACTION:
            
            break;
        case REMOVE_ACTION:

            break;
        default:
            break;
    }
}

export { reducer, initState };